# ── Database ──
DB_USERNAME=sergeguea
DB_PASSWORD=lamiapassword1

# ── JWT ──
JWT_SECRET=cambia-questa-chiave-segreta-in-produzione-min-256-bits

# ── CORS ──
CORS_ORIGINS=http://localhost:3000

# ── Mail (usa MailHog in sviluppo: porta 1025) ──
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=

# ── Storage ──
STORAGE_TYPE=local
# S3_BUCKET=espressamente-media
# S3_REGION=eu-south-1
# S3_ENDPOINT=http://localhost:9000
# S3_ACCESS_KEY=minioadmin
# S3_SECRET_KEY=minioadmin

# ── Notifiche ──
NOTIFICATION_EMAIL=info@espressamente.it
